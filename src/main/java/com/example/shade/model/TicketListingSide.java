package com.example.shade.model;

public enum TicketListingSide {
    /** Seller locks tickets; a buyer pays from wallet. */
    SELL,
    /** Buyer locks wallet money; a seller fulfills with tickets. */
    BUY_OFFER
}
